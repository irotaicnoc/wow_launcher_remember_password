Console command to build the executable:
    uv run pyinstaller --onefile --noconsole --name wow-launcher --icon="assets/wotlk_icon.ico" main.py

In the IDE console, uv is not required, so just:
    pyinstaller --onefile --noconsole --name wow-launcher --icon="assets/wotlk_icon.ico" main.py

